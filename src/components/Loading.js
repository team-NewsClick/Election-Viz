/**
 * Loading Spinner
 * @returns {JSX.Element} Loading-Spinner
 */
const Loading = () => {
    return (
        <div className="loading-container">
            <div className="loading-spinner" style={{fontSize: "12px"}} />
        </div>
    )
}

export default Loading
